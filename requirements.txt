# ─────────────────────────────────────────────
# Enterprise Knowledge Intelligence Platform
# requirements.txt  — Python 3.11.15
# ─────────────────────────────────────────────
#
# Conflict that was fixed:
#   cohere 5.5.4  → tokenizers>=0.15,<0.16
#   transformers  → tokenizers>=0.19,<0.20
# Fix: upgrade cohere to 5.11.4 (drops strict upper bound on tokenizers)
#      pin tokenizers==0.19.1 explicitly
#      upgrade transformers to 4.44.2 (same 0.19.x range, more stable on 3.11)
# ─────────────────────────────────────────────

# ── Core Framework ───────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Database ─────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# ── Async / Task Queue ───────────────────────
celery==5.4.0
redis==5.0.4
kombu==5.3.5

# ── Document Parsing (Day 2-3) ───────────────
pymupdf==1.24.3
pdfplumber==0.11.1
python-docx==1.1.2
unstructured==0.14.4
pytesseract==0.3.10
Pillow==10.3.0
easyocr==1.7.1
python-magic==0.4.27

# ── Chunking (Day 4-5) ───────────────────────
tiktoken==0.7.0
nltk==3.8.1
# spacy==3.7.4

# ── Tokenizers — PINNED EXPLICITLY ───────────
# Must be declared before transformers/cohere/anthropic
# so pip resolves this first.
# tokenizers 0.19.x is the intersection that satisfies:
#   transformers 4.44.2  → tokenizers>=0.19,<0.20  ✓
#   cohere 5.11.4        → tokenizers>=0.19         ✓
#   anthropic 0.34.2     → tokenizers>=0.13.0       ✓
tokenizers==0.19.1

# ── Embeddings (Day 6) ───────────────────────
# FIX: anthropic 0.28.0 → 0.34.2  (Python 3.11 stability)
anthropic==0.34.2
# FIX: openai 1.30.1 → 1.40.0
openai==1.40.0
# FIX: cohere 5.5.4 → 5.11.4  (removes tokenizers<0.16 constraint)
cohere==5.11.4
# FIX: sentence-transformers 3.0.0 → 3.1.1  (compatible with transformers 4.44)
sentence-transformers==3.1.1
# torch 2.3.1 — Python 3.11 wheel available on PyPI
torch==2.3.1
# FIX: transformers 4.41.1 → 4.44.2  (tokenizers>=0.19,<0.20; more stable on 3.11)
transformers==4.44.2

# ── Vector Database (Day 7) ──────────────────
qdrant-client==1.9.1

# ── Keyword Search (Day 9) ───────────────────
rank-bm25==0.2.2

# ── Agent Orchestration (Day 16-18) ──────────
# FIX: langgraph 0.1.1 → 0.2.16 + matching langchain-core
langgraph==0.2.16
langchain-core==0.2.38

# ── Evaluation (Day 20) ──────────────────────
# FIX: ragas 0.1.11 → 0.1.18  (compatible with langchain-core 0.2.x)
ragas==0.1.18
datasets==2.19.2

# ── Observability (Day 23) ───────────────────
langfuse==2.36.0
prometheus-client==0.20.0
structlog==24.2.0

# ── HTTP / Utilities ─────────────────────────
httpx==0.27.0
tenacity==8.3.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Config / Environment ─────────────────────
python-dotenv==1.0.1

# ── Data Utilities ───────────────────────────
numpy==1.26.4
pandas==2.2.2

# ── Testing ──────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# ── Dev Tools ────────────────────────────────
black==24.4.2
isort==5.13.2
mypy==1.10.0
ruff==0.4.4